$kind: http-request
url: "{{fineServiceBaseUrl}}/api/fines/{{fineId}}"
method: GET
headers:
  X-User-Id: "4"
scripts:
  - type: afterResponse
    code: pm.test('Status is 401', function () { pm.response.to.have.status(401); });
    language: text/javascript
order: 3000
