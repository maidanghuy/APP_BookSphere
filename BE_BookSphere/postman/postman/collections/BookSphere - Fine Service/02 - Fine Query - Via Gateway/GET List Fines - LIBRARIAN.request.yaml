$kind: http-request
url: "{{gatewayBaseUrl}}/api/fines?page=0&size=10"
method: GET
headers:
  Authorization: Bearer {{accessTokenLibrarian}}
queryParams:
  page: "0"
  size: "10"
scripts:
  - type: afterResponse
    code: pm.test('Status is 200', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 12000
